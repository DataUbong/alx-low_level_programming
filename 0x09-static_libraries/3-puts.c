#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * main - Entry point
 * _puts - Functions to insert string
 * @s: The string to be inserted
 * Return: Always 0 (Success)
 */
void _puts(char *s)
{
	puts(s);
}
